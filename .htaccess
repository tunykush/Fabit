RewriteEngine On
RewriteBase /Fabit/

# Redirect all requests to index.php, capturing the path as PATH_INFO
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [QSA,L]
